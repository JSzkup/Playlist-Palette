{
  "name": "color-playlists-visualizer",
  "version": "1.0.0",
  "description": "A client-side playlist visualizer that maps colors to music",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.19"
  },
  "dependencies": {
    "papaparse": "^5.5.2"
  }
}
